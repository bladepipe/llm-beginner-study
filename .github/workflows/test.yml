name: Test LLM Beginner Study
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install and test
        run: |
          pip install -e .
          python bigram_scratch.py
          python -c "from projects.minGPT_light.tokenizer import Tokenizer; print('✅ Tokenizer import OK')"
